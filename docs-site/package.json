{
  "name": "better-auth-server-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Documentation site for better-auth-server (Python port of better-auth).",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build && node scripts/check-anchors.mjs",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
